node_modules
.next
out/
dist
coverage
.nyc_output/
.env
.env.*
!.env.example

# Agent coordination cache
.agent-cache/
.claude/worktrees/

# Test output
test-results/
playwright-report/
.playwright-mcp/

# Supabase local runtime artifacts (not migrations)

# Editor
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db
desktop.ini

package-lock.json
yarn.lock
pnpm-debug.log
.pnpm-store/
*.log
*.tsbuildinfo
**/next-env.d.ts
*.bak

AGENTS.md

# Claude tooling lockfiles (ephemeral, not for commit)
.claude/*.lock

# E2E runner artifacts (screenshots, videos)
qa/e2e/*.png
qa/e2e/*.webm
qa/e2e/*.pdf

# Loose tooling artifacts (repo root)
/*.png
/*.pdf
/*.webm

# Local-only working docs
docs/AUDIT-*.md
.vercel

# Agent progress log (local-only, no longer tracked in git — see CLAUDE.md)
.claude/agent-progress.md

# Per-developer Claude settings (personal, never shared — .claude/settings.json stays tracked)
.claude/settings.local.json
# Added by code-review-graph
.code-review-graph/

# kx — managed, do not edit between these markers
.vercel
# kx — end
