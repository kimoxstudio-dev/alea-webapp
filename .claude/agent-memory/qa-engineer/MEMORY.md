- [scripts/*.mjs main() import guard](script-main-autorun-import-guard.md) — check for import.meta.url guard before testing a scripts/*.mjs file; missing guard = live process.exit/DB risk on mere import
- [fake-sql mock needs per-query-shape handler](fake-sql-mock-needs-per-query-shape-handler.md) — new source query text (DELETE/UPDATE/etc) needs its own dispatcher branch added before writing the test that exercises it
- [Verify regression test via git show](verify-regression-test-catches-bug-via-git-show.md) — confirm pre-fix commit really lacked the behavior, read-only, before trusting a green test as real coverage
- [Worktree missing .env.local blocks pre-push build](worktree-missing-env-local-blocks-prepush-build.md) — copy main checkout's .env.local (gitignored) into worktree before trusting a build "failure" as a regression
- [Clerk route-matcher mock needs case-insensitive flag](clerk-route-matcher-mock-must-be-case-insensitive.md) — hand-rolled createRouteMatcher mock without 'i' flag silently skips case-variant path scenarios
- [redirect mock doesn't throw](redirect-mock-does-not-throw-check-early-return.md) — check for explicit `return redirect(...)` before asserting nothing runs after a redirect branch
- [Neon migration: check cross-file callers](neon-migration-tests-also-check-cross-file-callers.md) — after migrating a service's own test file, grep other test files calling into it; stale Supabase mocks there attempt real Neon connections
- [Shared sql-mock needs sql.unsafe() support](shared-sql-mock-helper-needs-unsafe-support.md) — new sql.unsafe(...) column-list usage 500s every query through a try/catch until the shared mock helper implements .unsafe()
- [Neon time-column fallback regex trap](neon-time-column-fallback-regex-trap.md) — mock DB time columns as 'HH:MM:SS' (real Postgres wire format), not 'HH:MM', or a fallback-to-current-row regex bug hides itself
- [service-error mock needs real ServiceError class](mock-must-keep-real-error-class-alongside-mocked-factory.md) — omitting ServiceError from the mock makes `instanceof ServiceError` throw a Vitest proxy error, masquerading as an unrelated 500
- [describe brace off-by-one skips shared beforeEach](describe-brace-off-by-one-skips-shared-beforeeach.md) — a test passing alone but failing in the full file suggests a stray closing brace demoted a nested describe to a sibling, skipping the shared mock reset
- [Duplicated column-list constants drift](duplicated-column-list-constants-across-test-files-drift.md) — a hardcoded RETURNING/SELECT column-list mirror in a test file silently desyncs when the source list changes; give each distinct query its own constant, not one shared one
- [Shared sql-mock broad handlers need disambiguation](shared-sql-mock-broad-handlers-need-disambiguation.md) — createSqlMock helper factories matching table+verb only can silently eat a new rollback/read-back query shape; narrow the match or use prependHandler
- [Combined CTE insert+update needs one handler modeling both effects](combined-cte-insert-update-single-handler-must-model-both-effects.md) — WITH ins AS (INSERT...) UPDATE... is ONE mock call anchored to INSERT verb; handler must also simulate the UPDATE's mutation
- [`any(` substring conflates distinct query shapes](mock-any-substring-conflates-distinct-query-shapes.md) — new `= ANY(...)` query added to same table needs hasExactSelectColumns split, not shared `any(` substring match
- [Stale test encodes old error-message shape](stale-test-encodes-old-error-message-shape.md) — a "stop leaking error.message" fix round can leave a pre-existing green test asserting the old leaked value; must update it, not just add new tests
- [club-events addTablesHandler ordering trap](room-table-map-query-needs-priority-over-broad-tables-handler.md) — register the room->table map handler BEFORE addTablesHandler() in club-events-service.test.ts, or its column-agnostic match eats the roomTableMap query
